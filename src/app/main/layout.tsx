
export const metadata = {
  title: 'MAIN',
  description: 'Layout Enviar',
}

export default function MainLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (

    <div>

      {children}

    </div>

  )
}
